# 02 — 让工作负载索引分析仅支持 DTA 并完成清理

**What to build:** 将工单 01 建立的统一方法策略应用到 `analyze_workload_indexes`，使工作负载索引分析只接受 `dta`，并删除项目中剩余的 LLM 入口、占位消息、专用辅助逻辑、测试和用户文档。完成后，MCP Server、九个现有 MCP 工具、四种数据库的规则分析能力以及 Spring AI 提供的 MCP 基础设施保持可用。

**Blocked by:** 01 — 让查询索引分析仅支持 DTA

**Status:** resolved

- [x] `analyze_workload_indexes` 的工具名称、大小预算和 `method` 契约保持存在，MCP 参数说明只宣传 `dta` 及其默认行为。
- [x] `method` 为 `null`、空字符串或纯空白时被规范化为 `dta`，显式 `dta` 调用保持原有结果。
- [x] 任意非 `dta` 方法在 MCP 工具边界返回“仅支持 dta”的参数错误，且数据库方言和 SQL 客户端均未被调用。
- [x] PostgreSQL、MySQL、Doris 和达梦不再包含工作负载索引分析的 LLM 分支、占位消息或专用占位测试。
- [x] 删除不再被任何受支持行为使用的 LLM 判断辅助函数、私有方法和导入，不新增替代占位层。
- [x] 四种数据库原有的慢查询来源、扩展或功能检查、大小预算、规则分析和降级说明保持不变。
- [x] README 和兼容性说明只描述当前支持的 DTA/规则能力，不再保留 LLM 入口、占位返回或未来接入承诺。
- [x] 除本地需求规格和工单作为历史说明外，生产代码、测试、README 和兼容性文档不再包含 LLM 专用入口或占位内容。
- [x] 完整测试套件通过，并证明九个现有 MCP 工具仍被注册、MCP 应用上下文仍可启动、权限校验和数据库诊断行为没有回归。
- [x] Spring AI BOM、MCP Server starter、MCP 注解和服务配置保持不变，不因依赖名称包含 `ai` 而被删除。

## Comments

- 在 `DatabaseToolFacade` 统一规范化并校验工作负载索引分析方法，四种数据库方言只保留原有 DTA/规则路径。
- 删除剩余占位分支、辅助逻辑、专用测试及用户文档承诺；生产代码、测试、README 和兼容性文档的内容扫描无残留。
- 定向测试、编译和完整测试套件通过；Standards 与 Spec 双轴审查复核通过。
